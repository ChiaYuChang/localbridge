# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# built binaries (per-session `go build -o bin/`, never sealed)
bin/

# env
.env

# secrets (insurance: dir absent; blocks accidental commit if recreated)
.secrets/
